package com.education.stelar.academic.entity;

public enum PeriodType {
    SEMESTER,    // 2 por año (semestral)
    TRIMESTER,   // 3 por año (SEP básica)
    QUARTER      // 4 por año (cuatrimestral)
}
